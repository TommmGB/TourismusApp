import 'package:flutter/material.dart';

//First Route: AttractionsKleinBasel
class AttractionsKB extends StatelessWidget {
  const AttractionsKB({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Second Page'),
      ),
    );
  }
}
