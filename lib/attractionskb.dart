import 'package:flutter/material.dart';

class AttractionsKB extends StatelessWidget {
  const AttractionsKB({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Kleinbasel'),
      ),
    );
  }
}
